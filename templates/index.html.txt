<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Offline Hybrid Assistant</title>
    <style>
        body { font-family: Arial, sans-serif; background: #1e1e2e; color: white; text-align: center; padding-top: 50px; }
        .card { background: #2e2e3e; display: inline-block; padding: 30px; border-radius: 12px; width: 350px; }
        input { width: 80%; padding: 10px; margin-bottom: 10px; border-radius: 5px; border: none; }
        button { padding: 10px 15px; border-radius: 5px; border: none; background: #00adb5; color: white; cursor: pointer; }
        #output { margin-top: 20px; font-weight: bold; color: #00fff5; }
    </style>
</head>
<body>
    <div class="card">
        <h2>Hybrid Voice/Text Assistant</h2>
        <input type="text" id="userInput" placeholder="Type or say a command...">
        <br>
        <button onclick="sendCommand()">Send Command</button>
        <button onclick="startVoice()">🎤 Speak</button>
        <div id="output"></div>
    </div>

    <script>
        async function sendCommand() {
            let input = document.getElementById("userInput").value;
            let responseDiv = document.getElementById("output");
            responseDiv.innerText = "Processing...";

            let res = await fetch("/process", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ command: input })
            });
            let data = await res.json();
            responseDiv.innerText = data.response;
        }

        // Web Speech API for Local Voice Recognition
        function startVoice() {
            let recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
            recognition.onresult = function(event) {
                document.getElementById("userInput").value = event.results[0][0].transcript;
                sendCommand();
            };
            recognition.start();
        }
    </script>
</body>
</html>
